scalar JSON

type Ide {
  id: ID!
  # members: [Member!]
  content: String!
}

type IdeContentUpdate {
  changes: JSON!
}

# input changeIdeContentInput {
#   id: ID!
#   update: IdeContentUpdate!
# }

extend type Query {
  allIdes: [Ide!]!
  ide(id: ID!): Ide!
}

extend type Mutation {
  addIde: Ide!
  changeIdeContent(id: ID!, update: JSON!): Ide!
}

extend type Subscription {
  ideContentChanged(id: ID!): JSON!
}
